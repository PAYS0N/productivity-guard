services:
  backend:
    build: .
    env_file: .env
    ports:
      - "8800:8800"
    volumes:
      - /etc/productivity-guard/:/etc/productivity-guard/
      - ./backend/config.yaml:/app/config.yaml:ro
      - pg-data:/data
    restart: unless-stopped

volumes:
  pg-data:
